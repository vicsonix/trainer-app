**Target:** appointment `status` write-path — entry: `actions/appointments/index.ts:182` (`updateAppointmentStatusAction` — server guard + single `revalidatePath('/calendar')`) → `AppointmentDetailModal.tsx` (UI that mirrors the guard) → `analytics/page.tsx` + `dashboard/page.tsx` (both derive every KPI from the same `status` column) — selected because the map's risk zone marks this as the hardest current coupling: one writer, one DB enum, three independent read surfaces, and the write invalidates only `/calendar` (unknown U1: do analytics/dashboard show stale numbers?). Supersedes the prior AI-approval-flow target, now dormant after S-06 closed.

---
